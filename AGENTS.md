## Development Server Rule

- NEVER start, stop, restart, or manage the frontend or backend development servers.
- Do NOT run commands such as `npm run dev`, `npm start`, or backend server commands.
- The user is responsible for starting and stopping both the frontend and backend servers manually.
- If the application requires the frontend or backend to be running, ask the user to start it themselves.